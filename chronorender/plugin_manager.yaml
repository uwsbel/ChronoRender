factory: 
    geometry:
        paths: ./geometry
    renderobject:
        paths: ./renderobject
    renderpass:
        paths: ./renderpass
    rendersettings:
        paths: ./rendersettings
    shader:
        paths: ./shader
